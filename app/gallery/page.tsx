import GalleryClient from "./GalleryClient";

export const metadata = {
  title: "Gallery | Blue Skies School East Champaran",
  description:
    "Explore Blue Skies School gallery – students activities, campus, labs, sports and yoga sessions.",
};

export default function GalleryPage() {
  return <GalleryClient />;
}
